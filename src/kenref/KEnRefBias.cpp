/*
 * KEnRefBias.cpp — FROZEN FRAME (PLUMED fork)
 *
 * The implementation (including PLUMED_REGISTER_ACTION) lives in the KEnRef repository
 * (kenref_plumed: src/plumedinterface/KEnRefBias.cpp). This frame compiles it within PLUMED's build,
 * so the action is registered and the heavy/updatable logic stays in the KEnRef repo (permissive
 * license) — updating KEnRef never requires re-pushing this fork. The KEnRef repo's src/ must be on
 * the include path (see this module's Makefile: KENREF_SRC_REPO).
 */
#include "plumedinterface/KEnRefBias.cpp"
