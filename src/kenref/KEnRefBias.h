/*
 * KEnRefBias.h  — FROZEN FRAME (PLUMED fork)
 *
 * The KEnRefBias implementation lives in the KEnRef repository (kenref_plumed:
 * include_plumedinterface/plumedinterface/KEnRefBias.h + src/plumedinterface/KEnRefBias.cpp), under a
 * permissive license and as the single source of truth. This fork only compiles that source within
 * PLUMED's build and registers the action. The KEnRef repo's include_plumedinterface must be on the
 * include path (see this module's Makefile: KENREF_SRC_REPO).
 *
 * (No include guard here — the repo header carries its own.)
 */
#include "plumedinterface/KEnRefBias.h"
